name: Danger-Test

on:
  push:
    branches:
      - master
      - develop
      - feature/*
      - release/*

jobs:
  danger:

    runs-on: ubuntu-latest
    
    steps:
    - name: Danger JS Action
      uses: danger/danger-js@9.1.8
      with:
        args: --dangerfile ./Dangerfile.js
