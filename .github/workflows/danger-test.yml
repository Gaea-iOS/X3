name: Danger-Test

on:
  push:
    branches:
      - master
      - develop
      - feature/*
      - release/*

jobs:
  danger:

    runs-on: macOS-latest
    
    steps:
    - name: Danger Swift
      uses: danger/swift@2.0.3
